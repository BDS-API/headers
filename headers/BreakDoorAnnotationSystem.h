#pragma once

class BreakDoorAnnotationSystem : ITickingSystem {

    virtual void ~BreakDoorAnnotationSystem();
    virtual void ~BreakDoorAnnotationSystem();
    virtual void tick(EntityRegistry &);
}
