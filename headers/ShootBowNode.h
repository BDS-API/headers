#pragma once

class ShootBowNode : BehaviorNode {

    virtual void ~ShootBowNode();
    virtual void ~ShootBowNode();
    virtual void tick(Actor &);
    virtual void initializeFromDefinition(Actor &);
}
