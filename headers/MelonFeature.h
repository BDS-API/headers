#pragma once

class MelonFeature : Feature {

    virtual void Melon~MelonFeature();
    virtual void Melon~MelonFeature();
    virtual void place(IBlockPlacementTarget &, BlockPos const&, Random &)const;
    virtual void Melonplace(BlockSource &, BlockPos const&, Random &)const;
}
