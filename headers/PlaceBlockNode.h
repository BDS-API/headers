#pragma once

class PlaceBlockNode : BehaviorNode {

    virtual void ~PlaceBlockNode();
    virtual void ~PlaceBlockNode();
    virtual void tick(Actor &);
    virtual void initializeFromDefinition(Actor &);
}
